# Mobile

App Expo + React Native integrado à API real do projeto.

## Como apontar para a API
Crie um `.env` na pasta `mobile` com:

```env
EXPO_PUBLIC_API_URL=http://localhost:3002/api
```

Troque o host pelo IP local da máquina quando usar celular físico.
